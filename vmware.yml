- name: talk to esx
  hosts: Vmware
  become: False
  gather_facts: False
  tasks:
  - name: Gather vmware host facts
    vmware_host_facts:
    register: host_facts
    delegate_to: localhost
  - debug: msg="{{ host_facts }}"
